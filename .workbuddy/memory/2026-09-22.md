# 2026-09-22 工作日志

## 把冒烟脚本从 server/tmp 迁到 server/tests/smoke 并入库

### 起因
`server/tmp/` 整体被 gitignore，M2–M6 的端到端冒烟脚本一直游离在仓库外，
别人 clone 下来无法复现验收。用户要求「移到 server/tests/smoke/」。

### 入库范围（用户拍板：仅冒烟脚本）
入库：`_common.py`（新增）、`smoke_m2.py` … `smoke_m6.py`、`probe_types.py`、`dbq.py`。
**不入库**：`api.exe`/`worker.exe`/`importer.exe`、`*.log`、`smoke_m5.out`、
`ui/` 下的浏览器流程与截图、`dbhelp/*`、`mathcmp/`、`pdfprobe/` —— 均为一次性产物或二进制。

### 关键发现：脚本里写死了凭据（差点提交进仓库）
1. **6 个脚本全都硬编码了同一个测试账号口令**（13 字符，sha1 9c42ba），
   `smoke_m6.py` 另有 2 处正确 PIN 字面量。
2. **我第一次的凭据扫描是漏报的**（正则写法有问题，只报了 ui_setup.py）。
   换成正则更严谨、且**只打印长度与 sha1 指纹、绝不打印明文**的扫描器
   （`server/tmp/scan_creds.py`）后才扫全 —— 教训：安全扫描不能只跑一次就信。
3. `ui_setup.py` 与 `ui/flow4.json`/`flow4b.json`/`flow6.json` 也含凭据字面量；
   `ui_setup.py` 因触发安全策略无法读取，**本轮未入库**，待脱敏后再说。

### 做法：凭据外置 + 消除 CWD 依赖
- 用一次性脚本 `server/tmp/refactor_smoke.py` 把凭据值**直接从脚本抽到 `server/.env`**
  （`SMOKE_ACCOUNT` / `SMOKE_PASSWORD` / `SMOKE_PIN`），值全程不经过对话输出；
  `.env.example` 补三行占位 + 注释说明各自用途。
- PIN 的提取规则踩了一次坑：要**只认同一行同时有 `/auth/pin` 与 `expect=200` 的调用**，
  否则会把「非法格式 `abc`（422）」当成正确 PIN；故意错的 `0000`（401）同理必须保留字面量。
- 新增 `server/tests/smoke/_common.py`：按 `__file__` 推导 `SERVER_ROOT` / `TMP_DIR` / `ENV_PATH`，
  提供 `env(key, default=None)`。各脚本删掉自己那份 `env()` 定义，改为 `from _common import ...`。
  于是 **任意 CWD 都能跑**（旧写法 `open(".env")`、`dirname(dirname(__file__))` 迁完就指错）。
- `smoke_m4/m5` 的 `subprocess.run(..., cwd=".")` 改成 `cwd=SERVER_ROOT`：
  Go 侧用 `godotenv.Load()`，它是从**当前工作目录**找 `.env` 的，不固定 CWD 换个目录跑就会缺配置。
- `./tmp/worker.exe`、`./tmp/importer.exe` 改成 `os.path.join(TMP_DIR, ...)`。

### 一个真实的诊断盲点（已修）
`smoke_m5.py` 的 `run_bin` 只在 worker **非 0 退出**时打印日志。但 worker 单条渲染失败时
仍以 0 退出（只记 `failed` 计数），于是首次跑 M5 时 16 项失败却**看不到任何原因**。
已改成成功时也打印 worker 收尾摘要（`claimed=… ready=… failed=…`）。

### 迁移后实跑验证（全部从**仓库根目录**执行，专门验证任意 CWD）
| 脚本 | 结果 |
| --- | --- |
| smoke_m2.py | 36 通过 / 0 失败 |
| smoke_m3.py | 72 通过 / 0 失败 |
| smoke_m6.py | 94 通过 / 0 失败（与 9-21 基线一致） |
| smoke_m4.py | 106 通过 / 0 失败（走 worker.exe / importer.exe 新 cwd） |
| smoke_m5.py | **首次 16 项失败 → 复跑 107 通过 / 0 失败** |

M5 首次失败的归因：`print` 渲染器是**懒启动且失败会记住不重试**的，首次 Chromium 冷启动
没起来 → 任务回落 `queued` → 后续断言全挂。手动跑一次 worker 立刻 `claimed=2 ready=2 failed=0`，
复跑 107/107 全绿 —— 属偶发，与路径改造无关（M4 走同一条新 cwd 却全绿，可作旁证）。

### 本机环境备忘
- 用 `(cmd &)` 起的进程会随该次 Bash 调用结束被回收；要常驻必须用托管后台任务
  （本次 API 因此白起了一次，表现为后续 curl 连接被拒）。
- Vite dev server 只监听 `localhost:5173`，`127.0.0.1:5173` 不通 —— 与同站约定一致。
- `.env.example` 新增的 SMOKE_* 只是占位；真实值在 `server/.env`（gitignored）。

## UI 验收脚本脱敏并入库（server/tests/ui）

### 凭据反查推翻上一轮的判断
上一轮靠**键名**扫描（`password|pin|...`），既误报又漏报。这次改成**拿 .env 真实值当基准反查**
（`server/tmp/find_creds.py`），才拿到准确清单：`ui_setup.py` 3 处，`ui/flow*.json` **32 处**。
上一轮只认出 flow4/4b/6 —— 既漏了 flow1/2/5/7，又把 flow4 里 `input[type=password]`
这种 selector 字样当成了凭据。
**教训：安全扫描要拿真实值当基准，不能靠模式的键名猜。**

### 做法
- 一次性脚本 `server/tmp/refactor_ui.py`：从 `.env` 读值直接替换，**明文全程不经过对话输出**；
  每个文件替换后先 `json.loads` 校验，不合法就中止。
- 三类值参数化：凭据 → `${SMOKE_*}`；前端地址 → `${BASE_URL}`；
  截图目录（原为硬编码 `E:/Personal/...`）→ `${OUT_DIR}`。
- 新增 `run.py`：渲染后把 JSON 从 **stdin** 喂给 `agent-browser batch`（凭据不进命令行）。
- `_common.py` 从 `tests/smoke/` **上移到 `tests/`**，smoke 与 ui 共用；6 个 smoke 脚本补 `sys.path`。

### 踩到的三个坑（都是 run.py 自己引入的，已修）
1. **Windows 路径反斜杠破坏 JSON**：`${OUT_DIR}` 渲染成 `E:\Personal\...`，`\P`/`\s`/`\u`
   全是非法转义 → agent-browser 整批拒收，只报一句 `invalid escape at line N column M`。
   修法：替换值统一 `json.dumps(...)[1:-1]` 转义，并在启动浏览器**前**先 `json.loads` 本地校验。
2. **等 stdout EOF 会挂死**：agent-browser 拉起的 Chrome 继承了管道，agent-browser 退出后管道
   仍开着 → `for line in proc.stdout` / `communicate()` 永远等不到 EOF。最初那个
   「跑 3 分 38 秒、毫无输出」就是这个原因（不是流程卡住）。
   修法：读输出放独立线程，主线程只 `proc.wait()`；跑完默认 `close --all` 回收。
3. 顺带：`bail` 默认关（continue all），所以**退出码 0 不代表每条断言都成立** ——
   必须看输出里的 `active`/`clicked-*` 实际值。

### 流程脚本自身的两处失效（一并修掉）
- `flow1` 缺 PIN 解锁步骤（9-21 写时账号还没设 PIN）→ 停在选孩子页；
  且 `find text 语文 click` 是模糊匹配，首页「么语文/哥语文…」列表项会抢中，
  改成对 `button` 做精确匹配。
- `flow3`/`flow5` 按「数学题有选项」写的，但数学全是 `math_param` 填空、没有选项按钮，
  方向键断言从头不成立（退出码 0 掩盖了）。改走语文；数学作答由 `smoke_m3.py` 覆盖
  （那边能从题面算出答案做确定性断言）。

### 实跑结果
flow1 33→（加 PIN 后）实跑 0 失败、方向键序列 `1么 →(Down) 2爷 →(Down) 3奶 →(Up) 2爷`、
Enter 提交后进度 1/7 → 2/7；flow2 / flow3 / flow5 / flow7 均 exit=0 且断言非空。
flow4 / flow6（护眼休息页与到点锁屏，需各等 1 分钟 / 5.5 分钟）另跑。

### 环境备忘
- `agent-browser batch` 从 stdin 收 JSON 数组 `[["open","url"],...]`，与 flow 文件结构一致。
- 每个 batch = 一次新会话（daemon 重建会清 Cookie），所以**每个 flow 必须自带登录**。
- 「`(cmd &)` 起的进程随该次 Bash 调用结束被回收」再次验证：API 得用托管后台任务起才活住。


## 追加：flow4 / flow6 打通（长等待 + 日额度两处根因）

上一轮 flow4/flow6 一直 exit=1，这轮定位到**两个独立根因**，都已修掉：

### 根因 1：agent-browser 单条命令 25 秒硬上限
查 agent-browser README 确认：**标准操作（含 wait）默认超时 25 秒**，且受 CLI 侧
30 秒 IPC 读超时约束（`AGENT_BROWSER_DEFAULT_TIMEOUT` 调高到 30s 以上会 EAGAIN）。
所以 `wait <sel> --timeout 420000` 根本等不住 —— 25 秒后报
`Failed to read: A connection attempt failed...`（CDP 空闲断连），打 `✗` 并把退出码拖成非 0。
实测确认：批处理里单条命令失败**不会中断整批**（后续 `wait`/`eval` 照跑），但退出码变 1。

修法：**长等待改由 host 侧轮询**。`run.py` 重写为
1. 同一 `--session`（实测 `--session` 跨 CLI 调用**保页面/Cookie/localStorage**）跑前半批；
2. host 每 3 秒 `agent-browser --session <S> eval "!!document.querySelector('<sel>')"`，
   命中即返回（真早退，不白等满预算，也**不产生任何假失败**）；
3. 同一 `--session` 跑后半批。
`["wait","<ms>"]`（ms>25s）同理改成 host 分片盲等。流程 JSON 无需改结构 ——
`--timeout N` 在 run.py 里被解释成「host 侧轮询预算」。`--dry-run` 会打印分段计划。
另加：某批非 0 就直接停（不再傻等后面的长等待）。

顺手修的坑：
- `shutil.which("agent-browser")` 命中 npm 的 `.CMD` shim，`node <.cmd>` 报语法错 ——
  改成反推 `<node 目录>/node_modules/agent-browser/bin/agent-browser.js`（向上找 3 层）。
- 轮询耗时原来按「轮询次数 × 间隔」估算，漏了每次 eval 的进程开销（~1s/次），
  报告值偏低（300s 显示成 228s）→ 改用 `time.monotonic()` 真实计时。

### 根因 2：孤儿会话把当日时长顶爆
`TodayUsedSeconds` 对 `ended_at IS NULL` 的会话按 `now - started_at` 计，且**没有会话过期
回收**（只有 StartSession/FinishSession，worker 里没有 expire）。反复跑同一天的验收不断
留下未结束会话 → 当日时长从 66/60 涨到 216/480。`remaining<=0` 时组卷不再排新题、
会话可能起不来，护眼场景被日额度掩盖。

修法：`ui_setup.py` 的每条命令都把 `daily_limit_min` 给到**上限 480**（等同不设闸门），
并在 docstring 写明理由。日额度文案由后端 `planMessage` 承担，不在这几条浏览器流程里验。

### 结果（全部 exit=0、失败项 0 条）
- flow4：轮询 55.2s 命中休息页 → `restShown:true` / `countdown:"16"` → 点「我休息好了」
  → `pinDialog:true` → PIN 解锁 → `restShownAfterPin:false`。
- flow6：轮询 ~300s 命中锁屏 → `lockShown:true`（「这一节的时间到啦」）→ 家长解锁继续
  → `pinDialogShown:true` → PIN → `lockShownAfterPin:false` / `backToNormal:true`。
- 删除冗余的 `flow4b.json`（差点是 flow4 的旧写法）；给休息页倒计时加
  `data-testid="rest-countdown"` 并把它并进 flow4 断言。
- 前端 `npm run build` 通过（121 模块 / 294KB）。

## 追加：截图策略拍板（本地证据，不入库）

**决策**：UI 验收截图**不入库**，只作本地一次性证据；理由是 UI 后续还要改版，
版本化像素基线一改版就全红，维护成本落在「每次改版重刷基线」上，换不来额外信息。

现状核对（决策与现状本就一致，无需搬动文件）：
- 21 张验收 png 全在 `server/tmp/ui/`，被 `/server/tmp/` 覆盖；`git ls-files | grep .png` 为空；
- 全仓无任何像素/baseline 比对：`screenshot` 是指令数组里的叶子命令，输出从不被读回。

顺着这条决策补了两件事：
1. **把「结论不依赖截图」变成可验证的约束**：`run.py` 新增 `--no-shot`，在起浏览器
   **之前**把 `["screenshot", ...]` 指令全过滤掉。实测 flow2 两次跑（带截图 vs `--no-shot`），
   `matchMedia` 断点断言 `w:390/820/1440/1920 → sm/md/lg/xl` **逐字一致**，均 exit=0、
   失败项 0 条，且 `server/tmp/ui/` 一张 png 都没新增（21 → 21）。
2. **文档写明改版时的连带影响**：选元素一律用 `data-testid`（不要 Tailwind 类名或 DOM 层级），
   所以改版真正会打断的只有**少数文案断言**（flow4「让眼睛歇一会儿」、flow6「这一节的时间到啦」），
   改文案时同步这两条流程、锚点不动。落点：`server/tests/ui/README.md`（新增「截图策略」小节）
   + `docs/开发设计文档.md` §10.9。

MEMORY.md 已到 2995/3000 字符上限，为塞进这条约定做了两处无损压缩（npm 工具链块尾部、
前端入库提交策略），净增 0 字符。

**待办**：`server/tmp/ui/` 里还留着迁移前的 `flow1..7.json` / `flow4b.json` / `diag*.json`
（9/21，**含明文凭据**），已被 `server/tests/ui/` 取代。留着是脱敏的反面（磁盘上多 10 份明文副本），
但删除不可逆（从未入库、无历史可查），已向用户提出待确认。

## 追加：凭据闸门（tools/check_secrets.py + .githooks/pre-commit）

### 一、清理迁移前旧原件（用户确认「删掉」）

`server/tmp/ui/` 里还留着脱敏前的原件：`flow1..7.json` / `flow4b.json` / `diag*.json`（9/21）。
用真实 `.env` 值反查确认 **8 个文件、25 处明文凭据** —— 正是脱敏要消掉的东西换了个地方留在磁盘上。

已删（显式列名，不用通配符），复核 **0 处**；21 张 png 与 10 个 .log 原样保留。
这些文件从未入库（`/server/tmp/` gitignored），删了没有历史可查 —— 所以之前单独问过才动手。

### 二、把扫描器提炼进 `tools/`

原 `server/tmp/find_creds.py`（拿真实 `.env` 值反查，准）+ `scan_creds.py`（模式匹配）
合并成 `tools/check_secrets.py`，三道判定：

1. **真实值反查** —— 从 gitignored 的 `server/.env`/`web/.env` 读敏感键值（键名要求
   敏感词是**最后一段**，否则 `PIN_LENGTH` 这类配置值会疯狂误报），反查是否出现；
   短值（4 位 PIN）要求被引号包裹才算。
2. **模式匹配** —— JWT / Bearer / `password: "…"`，兜住「值不在本机 .env」的情况。
3. **策略** —— 文件名含 `.env` 且非 `.example` 不许入库；脚本/配置里不许有本机绝对路径。

只输出位置/键名/长度，**绝不打印明文值**。另有 `--path`（指定文件）、`--all`（全仓巡检）。
行内 `secretscan:allow` 可放行样板行（只对第 2、3 层生效）。

调过两处误报/自指：
- `kv-literal` 漏加 `(?<![-\w])`，把 `autoComplete={... ? 'current-password' : 'new-password'}`
  里的三元 `:` 当成了键值分隔符（`web/src/features/auth/LoginPage.tsx:94` 实际命中）。
- 本机路径层把 `.md` 与 `.workbuddy/` 豁免：那是开发笔记，常要写真实路径说明环境问题；
  而脚本里出现绝对路径换环境就炸，必须拦。
- `/cygdrive/` 那条正则自己命中自己，用 `secretscan:allow` 标注（正当用法）。

### 三、钩子与实证

`.githooks/pre-commit` → `exec python tools/check_secrets.py --staged`，`core.hooksPath=.githooks`。
两处防御：python 必须**真能跑**才用（Windows 上 `python` 可能是应用商店占位程序，
会把正常提交误判成有问题）；找不到 python 时大声告警但不阻塞。`SKIP_SECRET_SCAN=1` 应急绕过。

实证（不是「看起来对」）：
- 全仓 `--all`：208 个已跟踪文件 **0 命中**（顺带独立复核了「已入库脚本无明文凭据」）；
- 造探针（真实凭据由脚本从 .env 写入，不经终端）→ `git commit` **退出码 1、HEAD 未变、
  未产生提交**，三层判定都报了出来；探针随即删除并核对无残留；
- `SKIP_SECRET_SCAN=1` → 跳过并告警，退出码 0。

### 四、MEMORY.md 又顶到上限

3000 字符上限，这轮塞入钩子约定时先腾地方（删掉已过期的「前端入库提交」策略，
压缩 pgpass、VITE 同站、daysBetween、打印 `FOR UPDATE SKIP LOCKED` 等处的冗词），
最终 2977 —— README/设计文档才是细节的家，记忆只留「容易再犯的坑」。
