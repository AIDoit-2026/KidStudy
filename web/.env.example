# 前端环境变量模板（真实 .env 一律 gitignore）。
# 只放「非敏感」的构建期配置：任何写进 VITE_* 的值都会打进产物，绝不放密钥。

# 后端 API 基地址，必须含 /api/v1 前缀。
#
# ⚠️ 主机名必须与「浏览器访问前端的地址」保持同站，否则登录态会在换页后失效：
#    后端的 Refresh Token 是 HttpOnly + SameSite=Lax 的 Cookie，SameSite=Lax 的 Cookie
#    不会被**跨站**的 fetch/XHR 携带。而 localhost 与 127.0.0.1 在浏览器眼里是两个不同的站，
#    "localhost:5173 → 127.0.0.1:18080" 属于跨站，"localhost:5173 → localhost:18080" 才是同站。
#    因此这里统一用 localhost，且请用 http://localhost:5173 打开前端。
#
# 后端 CORS_ALLOWED_ORIGINS 也必须包含同样的来源（server/.env 里配 http://localhost:5173）。
# 本地开发：后端以 HTTP_ADDR=:18080 启动（8080 被 Jenkins 占用）。
VITE_API_BASE_URL=http://localhost:18080/api/v1
